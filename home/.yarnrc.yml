# Modern Yarn (Berry 2+) — global defaults for the image user.
# Classic Yarn 1 ignores this file; use .yarnrc for Yarn 1.
# Equivalent env is also set in the image (YARN_* → these keys).

enableGlobalCache: true

globalFolder: "/cache/yarn-global"

cacheFolder: "/cache/yarn-cache"
