# Yarn Berry (2+) — global defaults. Classic Yarn 1 ignores this file.
# Shared volume under /cache — no compose YARN_* required for these paths.

enableGlobalCache: true

globalFolder: "/cache/yarn-global"

cacheFolder: "/cache/yarn-cache"
