# Generated by bin/compose (UID/GID + absolute PROJECT_MOUNT)
.env
# Optional machine overrides (committed defaults live in .mise.env)
.mise.env.local
# base-image.env is NOT ignored — on GHE it is a normal tracked file so every
# internal clone gets BASE_IMAGE. Do not push that file to public github
# (bin/private-sync only pushes to PRIVATE_REMOTE). See docs/PRIVATE-BASE.md.

# Local compose overrides (ports, extra services)
compose.override.yml

# Sample warm / local install noise (packages live under /cache in the image)
node_modules/
.bundle/
vendor/bundle/
.venv/
__pycache__/
*.pyc
.python-version.local
# Written by cache-env --write-yarnrc during warm (points at /cache/yarn)
.yarnrc
.yarnrc.yml

# Editor / OS noise
*~
*.swp
*.swo
*.swn
Session.vim
.netrwhist
.DS_Store
.idea/
.vscode/
*.code-workspace

# Task / local tooling
.task/

# Image home/ seed templates (must ship with the image; not project warm noise)
!home/
!home/**
!home/.bundle/
!home/.bundle/**
!home/.yarnrc
!home/.yarnrc.yml
