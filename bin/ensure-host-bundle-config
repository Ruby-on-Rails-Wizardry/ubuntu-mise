#!/usr/bin/env bash
# Back-compat: ensure host ~/.bundle/config (and other package configs).
# Prefer: bin/ensure-host-package-config
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${ROOT}/ensure-host-package-config" "$@"
